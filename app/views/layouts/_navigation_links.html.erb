<%# add navigation links to this file %>
<% if user_signed_in? %>
  <li><%= link_to 'Logout', destroy_user_session_path, :method=>'delete' %></li>
<% else %>
  <li><%= link_to 'Login', new_user_session_path %></li>
<% end %>
<% if user_signed_in? %>
  <li><%= link_to current_user.email, edit_user_registration_path %></li>
  <% if current_user.role == "admin" %>
  	<li><%= link_to "Admin Panel", "/users/admin" %></li>
  <% end %>
<% else %>
  <li><%= link_to 'Add User', new_user_registration_path %></li>
<% end %>
<% if user_signed_in? %>
  <li><%= link_to 'Add User', '/users/new'  %></li>
  <!-- <li> <%#= collection_select("switch_user", "switch_user", User.all, :id, :email, {:prompt => "Switch User"}, {:onchange => "location.href = '/switch_user?scope_identifier=user_' + encodeURIComponent(this.options[this.selectedIndex].value) ", :class => "form-control", :style => "margin-top:10px; padding: 6px 6px; height:30px;" } ) %> </li> -->
<% end %>